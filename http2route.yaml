apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app2
  namespace: roboshop
spec:
  parentRefs:
    - name: roboshop
      sectionName: http

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /0
      backendRefs:
        - name: app2
          port: 80